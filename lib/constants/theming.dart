import 'package:flutter/material.dart';

class Theming extends StatelessWidget {
  const Theming({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //    final bgColor = MediaQuery.of(context).platformBrightness == Brightness.dark
    //     ? darkScaffoldBgColor
    //     : lightScaffoldBgColor;

    // final pattern = MediaQuery.of(context).platformBrightness == Brightness.dark
    //     ? 'assets/images/dark_pattern.png'
    //     : 'assets/images/pattern.png';
    // final logo = MediaQuery.of(context).platformBrightness == Brightness.dark
    //     ? 'assets/images/logo.png'
    //     : 'assets/images/logo_light.png';
    return Container();
  }
}
